name: Build CivilAI Desktop Installers

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-2022
            artifact: windows-installer
            command: powershell -ExecutionPolicy Bypass -File .\packaging\windows\build_windows.ps1
          - os: macos-14
            artifact: macos-arm64-installer
            command: bash packaging/macos/build_macos.sh
          - os: macos-13
            artifact: macos-intel-installer
            command: bash packaging/macos/build_macos.sh
          - os: ubuntu-22.04
            artifact: linux-installer
            command: bash packaging/linux/build_linux.sh

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf libasound2-dev

      - name: Build installer
        shell: bash
        run: ${{ matrix.command }}

      - name: Upload installer bundle
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            desktop/src-tauri/target/release/bundle/
          if-no-files-found: error
